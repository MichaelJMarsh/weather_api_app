name: weather_api_app
description: Basic implementation of Weather API.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flare_flutter: ^1.7.1
  flutter_bloc: ^7.1.0
  http: ^0.12.0+2
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/
